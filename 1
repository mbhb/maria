/*Ejercicio 2. 
Escribir un programa que muestre la política de planificación (como cadena) y la prioridad del proceso actual, 
además de mostrar los valores máximo y mínimo de la prioridad para la política de planificación.*/
#include <sched.h>
#include <stdio.h>

int main(){
    int policy = sched_getscheduler(0);
    switch(policy){
        case SCHED_FIFO : printf("Política de planificación: SCHED_FIFO\n"); break;
        case SCHED_RR : printf("Política de planificación: SCHED_RR\n"); break;
        case SCHED_OTHER : printf("Política de planificación: SCHED_OTHER\n"); break;
        default: printf("Ni idea"); break;
    }

    int minimo=sched_get_priority_min(policy);
    int maximo=sched_get_priority_max(policy);
    printf("Minimo %d y máximo %d\n", minimo, maximo);

    struct sched_param params;
    sched_getparam(0, &params);
    printf("Prioridad actual: %d\n", params.sched_priority);
    
    return 0;
}
