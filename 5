#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <stdio.h>
#include <errno.h>
#include <unistd.h>

int main()
{
    int fd = open ("archivo.txt", O_CREAT, 0645);
    if(fd==-1){
        perror("Error");
    } else{
        close(fd);
    }

    return 0;
}
